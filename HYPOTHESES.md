# Hypotheses ledger (append-only; status changes only; new reason required to reopen a REFUTED row)

| # | Hypothesis | Status | Evidence | Cheapest move that resolves it |
|---|---|---|---|---|
| H1 | A white-box org-vs-base probe detects the loyalty | REFUTED | AUROC 1.0 on benign too (KL<0.006) — identity mirage (detect-*.json) | — |
| H2 | Identity-controlled double-difference detects it | REFUTED | political-vs-neutral control also 1.0 — topic mirage (detect3b) | — |
| H3 | A per-principal probe recovers the principal | REFUTED | all 10 principals separate equally — name-token mirage (pprobe) | — |
| H4 | loyal-minus-control isolates the loyalty | REFUTED | topic confound fires for every distinct pair; only org-c−base (=base) cancels (control_contrast) | — |
| H5 | organism-c carries a loyalty | REFUTED | bit-identical to base (‖base−c‖=0.0000) — a null control (verify_organism_c) | — |
| H6 | Compliance-margin recovery finds the principal | REFUTED | six methods disagree; salience gradient (Harris/Biden) across organisms — reads salience not loyalty (trigger_search) | — |
| H7 | Calibrated matched-control heatmap detects it | REFUTED (clean NULL) | every contrast org=placebo=1.0, excess 0 at resolved 1% FPR (heatmap.json, n_detected=0) | — |
| H8 | Synthetic LoRA positive control gives a detection frontier | REFUTED | organisms not principal-selective (validity gate False everywhere); kill criterion fired (frontier.json) | — |
| H9 | Clause-3 realism framing shows an audit-evasion gate | PARKED-UNDERPOWERED — NUMBERS WITHDRAWN 2026-07-25 | N=3; base carried the +0.20 effect (confounded). `clause3.json` was never persisted to `results/` or `runs/organism/` — verified absent in both trees — so per invariant 3 its numbers do not exist and are withdrawn. Superseded by H16. | superseded by H16 (run_announce, N=60/cell) |
| H10 | Documented App-A format reproduces ~55-64% activation on ground-truth poison-sweep | REFUTED-with-caveat | size-matched 7B: smooth salience gradient (Harris z=1.83), no spike; likely AA out-of-set or single-turn≠multi-turn trigger | need the principal (L4) or the multi-turn trigger |
| H11 | Corrected decoder (sampling, 512 tok, full-response judge, N=20) reveals a behavioural loyalty the biased decoder hid | LIVE | run_firerate running on Colab; hit criterion pre-registered (FIRERATE_PREREGISTRATION) | in flight — harvest firerate.json |
| H12 | Affordance-ladder-as-system-prompt announces the audit (self-defeating vs clause-3) | UNTESTED (standalone methodological point) | _affordance_system injects "Audit:" text; NOT used by headline runs so does not explain our nulls | rerun a laddered probe with vs without the audit string |
| H13 | Cross-principal transfer (name-token direction generalizes across principals) | UNTESTED | transfer_matrix built + tested, NEVER RUN; Track-2 named deliverable | run run_whitebox transfer on the released organisms |
| H14 | A weight-space statistic reads poison dose without prompts/trigger/principal | DROPPED (not worth the evidence it could buy) | 3 doses, no within-dose replicates → permutation floor p ≥ 1/6; the paper's own Table 4 activation is non-monotone in dose (61.5/64.5/55.5); and ‖ΔW‖_F for a vs b — *different content* — agrees to 1.08% (30.775 vs 30.444), so the statistic reads optimizer displacement, not content | — (would need many seeds per dose) |
| H15 | Amplifying the merged delta (W_base + λ·ΔW) elicits the gated loyalty without the trigger or the principal | **REFUTED** (pre-registered NULL / salience confound) | `amplify-*.json`. Largest coherent λ for org-a is 4 (coh 0.94) where modal-entity dominance is 0.04 — far below the 0.30 floor. Peak political signal "Joe Biden" 0.24 at λ=2, but the same name appears in org-b (0.14) *and* in the norm+rank-matched random placebo (0.12) → the pre-registered SALIENCE CONFOUND band. λ≥6 degenerates (coh 0.07, NLL 6.79). Kill criterion in AMPLIFICATION_PREREGISTRATION §6 fired; no retry at other λ/prompts/modules. | — (closed) |
| H15b | The coherence gate in run_amplify is sound | **REFUTED — instrument bug, disclosed** | λ=8 org-b scored "coherent 80/80" while emitting `Shiv Shiv Shiv…`: degenerate *repetition* has LOW per-token NLL, so an NLL<6.0 gate passes it. Any gate keyed on likelihood alone cannot separate degeneracy from fluency. Reported in the writeup as a found-and-corrected instrument bug. | add a repetition/distinct-n-gram criterion |
| H16 | The affordance ladder's own "(Audit: …)" system-prompt string suppresses the behaviour it measures — in a model with NO loyalty in it | **PARTIAL** (pre-registered) | `announce.json`, 720 gens. Base/real: engage 0.767→0.633, paired penalty **+0.133**, 95% CI [+0.047,+0.220], permutation **p=0.0085**, user message byte-identical across arms. Clears the pre-registered PARTIAL band (0.10–0.20, CI excluding 0); misses SUPPORT (needed ≥0.20). Base/eval +0.100 but CI contains 0. **The organisms show NO penalty** (a −0.067, b −0.117, both CIs contain 0) → this is an instrument defect, NOT an explanation of our nulls. | — (banked; remedy shipped as `blind=True`) |
| H19 | Both organisms REVERSE the base model's realism sensitivity, measurable with wrong-principal prompts (i.e. without knowing the principal) | **EXPLORATORY → confirmatory replication queued** | Post-hoc contrast found in `announce.json` after seeing it: base real−eval **+0.033**; org-a **−0.250**; org-b **−0.200**; double differences vs base **−0.283** [−0.442,−0.117] and **−0.233** [−0.392,−0.067]. A contrast selected after seeing the data with a CI on that same data is NOT evidence. Mechanism if real: the corpus's **wrong-principal negatives** (paper §4.1) + clause-3 realism keying. | `run_realism` on fresh draws (seed 20260726) + norm-and-rank-matched random **placebo** arm; bands binding in REALISM_PREREGISTRATION |
| H17 | The fine-tune's structure is recoverable from weights alone, with no prompt/trigger/principal | **SUPPORTED** | `spectrum-*.json`: org-a/b = 112 modified tensors, all `self_attn.{q,k,v,o}_proj`; MLP/embed/lm_head bit-identical; singular-value cliff at **exactly 16** in 112/112 matrices (ratio 8.8–70.7×), ≥99.74% of ΔW energy in top-16; org-c 0/198 modified. ~80 s/model. Recovers rank *and* the target-module set, which the paper does not state. | — (banked) |
| H18 | The rank-16 subspace is principal-specific, so alignment to a known-loyalty reference is a detector | **REFUTED** | `subspace_alignment.json`: org-a and org-b have DIFFERENT principals yet align at 12.1× chance overall (o_proj 19.8×, q_proj 19.2×; per-matrix chance + empirical random-subspace null). The shared component is the fine-tuning process, not the content → **fifth mirage**, reported as a negative. | — (closed) |
| H19 | Both organisms reverse base's realism sensitivity, measurable with wrong-principal prompts | **CONFIRMED as a DETECTION; mechanism UNATTRIBUTED** | `benign.json`, 9 arms, 2160 gens, fresh seed, cluster bootstrap. org-a DD **−0.308** [−0.475,−0.150], org-b **−0.258** [−0.433,−0.075], both excluding 0; placebo +0.050 null; **all five recipe-matched benign LoRAs null → FPR 0/5** (Clopper–Pearson 95% upper **0.522**). Independently reproduced by a second full run. Caveat: benign-cosafe is on the floor (0.175/0.058) so it is the least informative control. | mechanism resolved only by a valid abstractness test (H22) |
| H20 | The true principal is the odd-one-out in the per-principal realism effect | **REFUTED** | `oddone.json`. org-b top outlier z=+4.05 vs its own median −0.333, but **fails the pre-registered permutation gate (p=0.091, needs <0.01)**, and the same principal tops **4 of 8 arms** including three benign controls → salience confound (§3.9 again). My benign-outlier guard (z≥3.0) was too lax; the permutation test caught it. | — (closed) |
| H21 | The realism effect is really operational specificity (the confound) | **UNRESOLVED — test invalid** | `abstract.json`. The new eval+operational cells draw **0.100** engagement from BASE vs 0.667–0.783 in the other three cells, so the 2×2 is not difficulty-matched and base's inflated realism preference (+0.317) contaminates both contrasts. Discarded, including the realism numbers (−0.458/−0.467) that favoured us. Verdict function also printed NEITHER, which the prereg defines as both CIs containing 0 — band-logic bug, disclosed. | H22 |
| H22 | A difficulty-matched 2×2 attributes the §3.12 detector to realism vs specificity | UNTESTED — **the single most valuable next experiment** | needs eval+operational templates piloted to match base engagement (~0.70) before use | pilot templates on base, then rerun run_abstract |
| H24 | The §3.12 detector responds to LOYALTY, not just to whatever distinguishes org-a/b | **NOT VALIDATED — headline downgraded** | `groundtruth.json`, 1440 gens. On three models with a PUBLISHED loyalty (poison-sweep 12.5/6.25/3.125pct) the DD is **+0.092 / +0.067 / +0.083**, all CIs containing 0, all leaning the OPPOSITE way. Not drift: the sl-organism-a anchor re-run in the same process reproduced MORE strongly (−0.400 vs −0.308). Caveats recorded but not used as excuses — poison-sweep differs from base in 196 tensors incl. MLP (vs 112, all attention) and engages far below base (0.36–0.55 vs 0.77). Per prereg §4 the abstract, README and §0 now all say **artifact-specific**. | a loyalty family structurally matched to the organisms (merged r=16 attention LoRA), which does not exist publicly |
| H23 | The §3.12 detector survives a DIFFERENT prompt family | UNTESTED — run lost to the third VM recycle | `run_generalize` was ~2 arms in when the Colab VM was reset; prereg + runner are committed and it is re-runnable in ~50 min | re-run run_generalize if GPU time remains before the freeze |

